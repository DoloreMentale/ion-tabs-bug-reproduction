import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-tabs-bug-reproduction',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
